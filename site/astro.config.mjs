// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://cody1991.github.io',
  base: '/learning/',
  trailingSlash: 'always',
});
