# Learning Hub (Astro)

静态站，渲染仓库根下各学习线的 Markdown。

```bash
npm install          # Node ≥ 22
npm run dev
npm run build        # 含 Pagefind 搜索索引
npm run preview
```

新学习线：在仓库根新建目录并放上 `track.json` + `README.md`，无需改本目录代码。
