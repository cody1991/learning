---
description: Git 提交与推送习惯——说提交就直接做，不要反复确认
alwaysApply: true
---

# Git 工作流

- **未要求时**：不要主动 commit / push。
- **用户说「提交」「提交代码」「推送」等**：立刻 `commit`，并 **`git push` 到当前跟踪分支**；不要再问「要不要推送」「确认提交吗」。
- 提交说明简洁，聚焦 why；不要提交 `.env`、密钥、labs 上游克隆等敏感/体积大内容。
- 破坏性操作（force push、hard reset 等）仍须用户明确点名才做。
